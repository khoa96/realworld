const loginInitialState = {
    
}
const loginReducer = (state = loginInitialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default loginReducer;